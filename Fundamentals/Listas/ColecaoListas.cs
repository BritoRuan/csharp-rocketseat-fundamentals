
namespace Fundamentals.Listas;

class ColecaoListas
{
    // Listas de inteiros
    // Não é necessario definir o tamanho da lista
    // Lista possui apenas uma dimensão

    List<int> inteiros = new List<int>();

    // Adicionando valores a listas

    // inteiros.Add(1);
    // inteiros.Add(2);
    // inteiros.Add(3);

    // Console.WriteLine(inteiros.Count); // 3

    // inteiros.Remove(1);

    // Console.WriteLine(inteiros.Count); // 2
}
