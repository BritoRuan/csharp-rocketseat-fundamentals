
namespace HelloWorld.HashSet;

class ColecaoHashSet
{
    // Lista que não permite elementos duplicados, ou seja, se você tentar adicionar um elemento que já existe, ele não será adicionado
    HashSet<int> inteiros = new HashSet<int>();

    //inteiros.Add(1);
    //inteiros.Add(2);
}
