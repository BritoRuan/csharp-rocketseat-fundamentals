
namespace HelloWorld.Arrays;

class ColecaoArrays
{
    // Arrays são coleções de elementos de um mesmo tipo e tamanho fixo
    int[] inteiros = new int[20]; // Array de inteiros com 20 posições

    // inteiros[0] = 1; // Atribuindo valor ao primeiro elemento do array

    // Console.WriteLine(inteiros[0]); // Acessando o primeiro elemento do array

    // Arrays multidimensionais

    int[,] arrayComDuasDimensoes = new int[2, 2]; // Array de duas dimensões 

    // inteiro[0,0] = 1; // Atribuindo valor ao primeiro elemento do array
}
