
namespace Fundamentals.Condicionais;

class Condicionais
{
    /*
     * > Maior
     * < Menor
     * >= Maior ou igual
     * <= Menor ou igual
     * != // is not Diferente
     * == // is Igual
     * && E
     * || OU
    */


    int numero = 20;

    /* 
    
    if (numero > 0) {
      Console.WriteLine("O número é positivo");
    } else if (numero == 0){
      Console.WriteLine("O número é neutro");
    } else {
      Console.WriteLine("O número é negativo");
    }
   
    */

    List<string> nomes = new List<string>() { "Ruan", "Mateus", "Samuel" };

    /*
     
    if (nomes.Contains("Lucas"))
    {
      Console.WriteLine("Lucas está na lista");
    }
    else
    {
        Console.WriteLine("Lucas não está na lista");
    }

    */
}
